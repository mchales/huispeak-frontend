import { HomeView } from 'src/sections/home/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'HuiSpeak',
};

export default function Page() {
  return <HomeView />;
}
